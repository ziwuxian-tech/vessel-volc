# vessel-volc

OpenCode 沙箱镜像，用于 Agent 代码执行环境。

## 镜像信息

- 镜像地址: `dev-container-cn-beijing.cr.volces.com/vessel/vessel-volc-opencode`
- 标签: `latest`, `vX.X.X` (版本号)

## 构建镜像

```bash
docker build -t vessel-volc-opencode .
```

## 运行镜像

```bash
docker run -it dev-container-cn-beijing.cr.volces.com/vessel/vessel-volc-opencode:latest
```
